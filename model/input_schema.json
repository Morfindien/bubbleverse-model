{
  "calculation_input_contracts": {
    "difference": "a,b OR JSON {a,b}",
    "gaussian-tension": "a,sigma_a,b,sigma_b OR JSON object with those keys",
    "percent-shift": "from,to OR JSON {from,to}",
    "weighted-mean": "value1,sigma1,value2,sigma2,... OR JSON {values:[...],sigmas:[...]}"
  },
  "input_fields": [
    {
      "name": "input",
      "purpose": "One user-supplied JSON object or comma-separated numeric input.",
      "required": false,
      "type": "string"
    },
    {
      "name": "operation",
      "purpose": "Select one supported model/calculation operation.",
      "required": true,
      "type": "choice"
    }
  ],
  "interface": "GITHUB_WORKFLOW_DISPATCH",
  "operations": [
    "test",
    "gaussian-tension",
    "difference",
    "percent-shift",
    "weighted-mean",
    "validate-model",
    "model-status",
    "contradictions",
    "provenance",
    "release-handoff",
    "campaign"
  ],
  "schema_version": 1
}
